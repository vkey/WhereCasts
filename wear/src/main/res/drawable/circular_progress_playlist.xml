<?xml version="1.0" encoding="utf-8"?>

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="0"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">

            <shape
                android:innerRadiusRatio="6.5"
                android:innerRadius="18dp"
                android:shape="ring"
                android:thicknessRatio="20.0"
                android:useLevel="true">

                <rotate
                    android:fromDegrees="0"
                    android:pivotX="50%"
                    android:pivotY="50%"
                    android:toDegrees="360" />

                <gradient
                    android:centerColor="@color/wc_playlist_progress"
                    android:endColor="@color/wc_playlist_progress"
                    android:startColor="@color/wc_playlist_progress"
                    android:type="sweep" />

            </shape>
        </rotate>
    </item>
</layer-list>